{
  "name": "byteplus-rtc-aigc-demo",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n server,web \"yarn --cwd server dev\" \"yarn --cwd web dev:flexible\"",
    "test": "yarn --cwd server test && yarn --cwd web test --watchAll=false --passWithNoTests"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "packageManager": "yarn@1.22.22"
}
